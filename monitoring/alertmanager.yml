global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'sns'

receivers:
  - name: 'sns'
    webhook_configs:
      - url: 'http://sns-forwarder:5010/alert'
        send_resolved: true