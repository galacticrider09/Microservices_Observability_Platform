groups:
  - name: service_health
    interval: 30s
    rules:

      # Alert if any service goes completely down
      - alert: ServiceDown
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "🔴 Service {{ $labels.job }} is DOWN"
          description: "{{ $labels.job }} has been unreachable for over 1 minute."

      # Alert if order-service error rate spikes
      - alert: HighOrderServiceErrorRate
        expr: rate(order_service_errors_total[2m]) > 0.05
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "⚠️ High error rate in order-service"
          description: "order-service is seeing {{ printf \"%.2f\" $value }} errors/sec over the last 2 minutes."

      # Alert if response time is too slow
      - alert: SlowResponseTime
        expr: histogram_quantile(0.95, rate(order_service_request_duration_seconds_bucket[5m])) > 1.0
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "🐢 order-service p95 latency > 1s"
          description: "95th percentile response time is {{ printf \"%.2f\" $value }}s."